version: "3"
   
services:
  db:
    build: ./db_instance/
    environment:
      - POSTGRES_PASSWORD=test_pass
    ports: 
      - 5432:5432
  web:
    build: .
    command: python main.py
    volumes:
      - ./src/:/code
    depends_on:
      - db